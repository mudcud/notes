
 sudo docker run -d \
         --name webmap \
         -h webmap \
         -p 8000:8000 \
         -v /tmp/webmap:/opt/xml \
         reborntc/webmap

 # now you can run Nmap and save the XML Report on /tmp/webmap
 nmap -sT -A -T4 -oX /tmp/webmap/myscan.xml 192.168.1.0/24

 sudo docker exec -ti webmap /root/token


 public curl ifconfig.co

private ifconfig